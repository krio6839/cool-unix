<template>
	<view class="flex flex-col items-center px-8">
		<cl-text color="white" :size="20" :pt="{ className: 'font-bold mb-12 mt-12' }">{{
			t("选择你的产品")
		}}</cl-text>
		<view class="mb-24">
			<image src="/static/icon/product-icon.svg" class="w-[100px] h-[140px]"></image>
		</view>
		<cl-button
			:pt="{ className: '!h-[45px] !w-[280px]', label: { className: 'device-button-label' } }"
			color="#000"
			type="primary"
			rounded
			@tap="onConfirm"
		>
			{{ t("确认") }}
		</cl-button>
	</view>
</template>

<script setup lang="ts">
import { t } from "@/.cool";

const emit = defineEmits(["click"]);

function onConfirm() {
	emit("click");
}
</script>

<style lang="scss" scoped>
.device-button-label {
	font-size: 17px;
	font-weight: 700;
}
</style>
