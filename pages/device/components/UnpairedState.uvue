<template>
	<view class="flex-1 flex flex-col items-center px-8">
		<view class="my-16">
			<image src="/static/icon/product-icon.svg" class="w-[100px] h-[140px]"></image>
		</view>
		<cl-text color="white" :size="14" :pt="{ className: 'mb-16 text-center' }">
			{{ t("未有设备连接，点击进入设备连接") }}
		</cl-text>
		<cl-button
			:pt="{ className: '!h-[45px] !w-[280px]', label: { className: 'device-button-label' } }"
			color="#000"
			type="primary"
			rounded
			:loading="loading"
			:disabled="loading"
			@tap="enterPairingMode"
		>
			{{ t(loading ? "正在准备..." : "去配对") }}
		</cl-button>
	</view>
</template>

<script setup lang="ts">
import { t } from "@/.cool";

defineProps<{
	loading: boolean;
}>();

const emit = defineEmits(["click"]);

function enterPairingMode() {
	emit("click");
}
</script>

<style lang="scss" scoped>
.device-button-label {
	font-size: 17px;
	font-weight: 700;
}
</style>
