import { IBluetooth, InitConfig } from '../interface';
import { BluetoothManager } from './bluetoothManager';

export function useKuxBluetooth (options: InitConfig = {} as InitConfig) : IBluetooth {
	return new BluetoothManager(UTSAndroid.getAppContext()!, options);
}

