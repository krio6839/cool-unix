import { IBluetooth, InitConfig } from '../interface';
import { BluetoothManager } from './bluetoothManager';

export function useKuxBluetooth (options?: InitConfig) : IBluetooth {
	return new BluetoothManager(options);
}
